# Findings ignored by osv-scanner with a documented reason and an expiry.
# When ignoreUntil passes, the scan fails again and the decision is revisited.

[[IgnoredVulns]]
id = "PYSEC-2026-311"
ignoreUntil = 2026-12-31T00:00:00Z
reason = "chromadb 1.5.9 is the latest published release and the advisory's affected range ends at 1.5.9, so no upgrade path exists. SermonPilot uses the embedded PersistentClient only; the advisories target the chromadb HTTP server, which this app never runs. Revisit when upstream publishes a fix."

[[IgnoredVulns]]
id = "PYSEC-2026-3813"
ignoreUntil = 2026-12-31T00:00:00Z
reason = "Same chromadb situation: no fixed release exists, embedded client only, server mode unused."

[[IgnoredVulns]]
id = "PYSEC-2026-3814"
ignoreUntil = 2026-12-31T00:00:00Z
reason = "Same chromadb situation: no fixed release exists, embedded client only, server mode unused."

[[IgnoredVulns]]
id = "PYSEC-2026-3815"
ignoreUntil = 2026-12-31T00:00:00Z
reason = "Same chromadb situation: no fixed release exists, embedded client only, server mode unused."

[[IgnoredVulns]]
id = "GHSA-rrmf-rvhw-rf47"
ignoreUntil = 2026-12-31T00:00:00Z
reason = "torch memory corruption via torch.jit.script, fixed in 2.13.0. The locked CPU torch is 2.12.1 and the app never uses torch.jit.script. The GPU images pin torch 2.6.0+cu124 because the CUDA 12.4 wheel index ends at 2.6.0; moving the GPU stack to a newer CUDA platform is tracked separately. Revisit with the CUDA migration."

# Not scanned automatically: requirements/requirements-gpu.txt and
# requirements-rocm.txt pin torch with +cu124/+rocm local versions that
# osv-scanner cannot resolve. The GPU images therefore run torch 2.6.0+cu124,
# which is additionally affected by PYSEC-2026-139, PYSEC-2026-2286, and
# GHSA-qfhq-4f3w-5fph (all fixed in torch 2.10.0+, unavailable on cu124).
# The CUDA platform migration is the remediation path for that exposure.
